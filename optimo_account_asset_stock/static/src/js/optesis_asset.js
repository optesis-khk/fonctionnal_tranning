odoo.optesis = function(instance){
	/*instance.optesis = {};
	var module = instance.optesis;

	local.asset = instance.Widget.extend({
	    start: function(){
	        console.log("the page has been loaded!!!");
	    },
	});

	module.widget = instance.web.Widget.extend({
	    init: function(parent,options){
	        this._super(parent);
	        options = options || {};
	        console.log("the widget has been initialised");
	    },

	    start: function(){
	        console.log("the widget has been started");
	    }
	});

	instance.web.client_actions.add('local.asset', 'instance.win001');*/

	/*

	instance.optesis.Asset = instance.web.Class.extend({
    test_key : function(e){
    	if(e.keyCode == 45){
    		alert("the button has been changed");
    	}
    	console.log("the code is started!!!");
    },
    });

    var listen = new instance.optesis.Asset();
    listen.test_key(e);*/
}